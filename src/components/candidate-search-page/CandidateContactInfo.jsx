import React from 'react';
import ContactInfoSection from '../ContactInfoSection';

const CandidateContactInfo = ({ candidate, bio }) => (
  <ContactInfoSection candidate={candidate} bio={bio} />
);

export default CandidateContactInfo;
